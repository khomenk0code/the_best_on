import styled from "styled-components";
import {Connect} from "./connect"

export const FooterButton = styled(Connect)`
  border: 1px solid #FFFFFF;
  background: #0D316D;
  margin-bottom: 14px;

  &:hover {
    cursor: pointer;
    background: #F1B634;
  }
`